<UserControl x:Class="TempMaster.Desktop.Controls.MeterCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:TempMaster.Desktop.Controls">
    <Border Margin="8"
            Background="White"
            BorderBrush="{StaticResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.12" Color="#000000" />
        </Border.Effect>
        <StackPanel>
            <!-- Header -->
            <Border Background="{StaticResource CardHeaderBrush}"
                    BorderBrush="{StaticResource CardBorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="4,4,0,0"
                    Padding="12,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="NameText"
                               FontWeight="Bold"
                               FontSize="14"
                               TextTrimming="CharacterEllipsis"
                               VerticalAlignment="Center" />
                    <Border Grid.Column="1"
                            Background="#EEEEEE"
                            CornerRadius="9"
                            Padding="8,2"
                            VerticalAlignment="Center">
                        <TextBlock x:Name="TypeText"
                                   FontSize="11"
                                   Foreground="{StaticResource MutedBrush}" />
                    </Border>
                </Grid>
            </Border>

            <!-- Body -->
            <StackPanel Margin="12,10,12,12">
                <StackPanel x:Name="StatsPanel"
                            Orientation="Horizontal"
                            Margin="0,0,0,10">
                    <Border x:Name="TempBadge"
                            Background="{StaticResource TempBrush}"
                            CornerRadius="3"
                            Margin="0,0,6,0">
                        <TextBlock x:Name="TempText" Style="{StaticResource BadgeText}" />
                    </Border>
                    <Border x:Name="HumidityBadge"
                            Background="{StaticResource HumidityBrush}"
                            CornerRadius="3"
                            Margin="0,0,6,0">
                        <TextBlock x:Name="HumidityText" Style="{StaticResource BadgeText}" />
                    </Border>
                    <Border x:Name="BatteryBadge"
                            Background="{StaticResource BatteryBrush}"
                            CornerRadius="3">
                        <TextBlock x:Name="BatteryText" Style="{StaticResource BadgeText}" />
                    </Border>
                </StackPanel>

                <controls:TemperatureChart x:Name="Chart" Height="200" />

                <TextBlock x:Name="LastUpdatedText"
                           Foreground="{StaticResource MutedBrush}"
                           FontSize="12"
                           Margin="0,8,0,0" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
