# Production build API base URL.
# Data is served by the snakeroom backend, which actively collects readings
# from the same SwitchBot devices, so the built frontend points all API calls
# there (matching the previous jQuery frontend behavior).
VITE_API_URL=https://snakeroom.fly.dev
