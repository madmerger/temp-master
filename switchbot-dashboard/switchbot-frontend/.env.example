# Base URL for the backend API.
# Leave empty (default) to use the same origin as the frontend (relative paths).
# Set to a full URL (e.g. https://temp-master.fly.dev) to point at a remote backend.
VITE_API_URL=
