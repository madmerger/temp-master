# Backend API base URL used by the dashboard.
# Data is served by the snakeroom backend, which collects readings from the
# same SwitchBot devices. Leave empty to call the same origin that serves the app.
VITE_API_URL=https://snakeroom.fly.dev
