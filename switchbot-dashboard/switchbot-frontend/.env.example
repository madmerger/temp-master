# Base URL for the backend API.
# Leave empty to use the same origin (default). In dev, requests to /api are
# proxied to http://localhost:8000 by vite.config.ts.
# Example overrides:
#   VITE_API_URL=https://snakeroom.fly.dev
#   VITE_API_URL=https://temp-master.fly.dev
VITE_API_URL=
