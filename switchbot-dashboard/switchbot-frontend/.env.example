# Optional: override the API base URL used by the frontend.
# Leave empty to use the same origin (recommended when served by the backend).
# During `npm run dev`, requests to /api are proxied to the backend (see vite.config.ts).
VITE_API_URL=
